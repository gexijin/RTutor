% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_helpers.R
\name{create_chat_completion_azure}
\alias{create_chat_completion_azure}
\title{Create Chat Completion Azure}
\usage{
create_chat_completion_azure(
  model,
  api_version,
  messages = NULL,
  temperature = 0.2,
  top_p = 0.95,
  n = 1,
  stream = FALSE,
  stop = NULL,
  max_tokens = NULL,
  presence_penalty = 0,
  frequency_penalty = 0,
  logit_bias = NULL,
  user = NULL,
  openai_api_key = Sys.getenv("AZURE_OPENAI_API_KEY"),
  endpoint = Sys.getenv("AZURE_OPENAI_API_ENDPOINT"),
  seed = NULL
)
}
\arguments{
\item{model}{required; a length one character vector.}

\item{messages}{required; defaults to `NULL`; a list in the following
format: `list(list("role" = "user", "content" = "Hey! How old are you?")`}

\item{temperature}{required; defaults to `1`; a length one numeric vector
with the value between `0` and `2`.}

\item{top_p}{required; defaults to `1`; a length one numeric vector with the
value between `0` and `1`.}

\item{n}{required; defaults to `1`; a length one numeric vector with the
integer value greater than `0`.}

\item{stream}{required; defaults to `FALSE`; a length one logical vector.
**Currently is not implemented.**}

\item{stop}{optional; defaults to `NULL`; a character vector of length
between one and four.}

\item{max_tokens}{required; defaults to `(4096 - prompt tokens)`; a length
one numeric vector with the integer value greater than `0`.}

\item{presence_penalty}{required; defaults to `0`; a length one numeric
vector with a value between `-2` and `2`.}

\item{frequency_penalty}{required; defaults to `0`; a length one numeric
vector with a value between `-2` and `2`.}

\item{logit_bias}{optional; defaults to `NULL`; a named list.}

\item{user}{optional; defaults to `NULL`; a length one character vector.}

\item{openai_api_key}{required; defaults to `Sys.getenv("OPENAI_API_KEY")`
(i.e., the value is retrieved from the `.Renviron` file); a length one
character vector. Specifies OpenAI API key.}

\item{seed}{optional; defaults to `NULL`; a length one
numeric vector with integer values. Attempts deterministic sampling.}

\item{openai_organization}{optional; defaults to `NULL`; a length one
character vector. Specifies OpenAI organization.}
}
\value{
Returns a list, elements of which contain chat completion(s) and
  supplementary information.
}
\description{
Creates a completion for the chat message. See [this
page](https://platform.openai.com/docs/api-reference/chat/create) for
details.
}
\details{
For arguments description please refer to the [official
documentation](https://platform.openai.com/docs/api-reference/chat/create).
}
\examples{
\dontrun{
create_chat_completion_azure(
   model = "gpt-4o-mini",
   api_version = "2023-03-15",
   messages = list(
       list(
           "role" = "system",
           "content" = "You are a helpful assistant."
       ),
       list(
           "role" = "user",
           "content" = "Who won the world series in 2020?"
       ),
       list(
           "role" = "assistant",
           "content" = "The LA Dodgers won the World Series in 2020."
       ),
       list(
           "role" = "user",
           "content" = "Where was it played?"
       )
   )
)
}
#export
}
